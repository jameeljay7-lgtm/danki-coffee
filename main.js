const products=[{name:'Medium Roast',size:'Beans · 1kg',price:30000,type:'beans medium',img:'assets/bag.svg'},{name:'Medium Roast',size:'Beans · 500g',price:15500,type:'beans medium',img:'assets/bag.svg'},{name:'Medium Roast',size:'Ground · 1kg',price:32000,type:'ground medium',img:'assets/bag.svg'},{name:'Medium to Dark',size:'Beans · 1kg',price:30000,type:'beans dark',img:'assets/bag.svg'},{name:'Medium to Dark',size:'Beans · 500g',price:15500,type:'beans dark',img:'assets/bag.svg'},{name:'Medium to Dark',size:'Beans · 250g',price:10000,type:'beans dark',img:'assets/bag.svg'},{name:'Medium to Dark',size:'Ground · 1kg',price:32000,type:'ground dark',img:'assets/bag.svg'},{name:'Medium to Dark',size:'Ground · 500g',price:17000,type:'ground dark',img:'assets/bag.svg'},{name:'Medium to Dark',size:'Ground · 250g',price:11000,type:'ground dark',img:'assets/bag.svg'},{name:'Dark Roast',size:'Beans · 1kg',price:30000,type:'beans dark',img:'assets/bag.svg'},{name:'Dark Roast',size:'Beans · 500g',price:15500,type:'beans dark',img:'assets/bag.svg'},{name:'Dark Roast',size:'Ground · 1kg',price:32000,type:'ground dark',img:'assets/bag.svg'}];let cart=JSON.parse(localStorage.getItem('danki-cart')||'[]');const money=n=>n.toLocaleString('en-US')+' TZS';function renderProducts(filter='all'){const el=document.getElementById('products');el.innerHTML=products.filter(p=>filter==='all'||p.type.includes(filter)).map((p,i)=>`<article class="product"><div class="product-image"><span class="tag">${p.size.split(' · ')[0].toUpperCase()}</span><img src="${p.img}" alt="${p.name} ${p.size}"></div><div class="product-body"><h3>${p.name}</h3><p>${p.size}</p><div class="price">${money(p.price)}</div><button class="btn primary" onclick="add(${i})">Add to bag +</button></div></article>`).join('')}function filterProducts(f,btn){document.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));btn.classList.add('active');renderProducts(f)}function add(i){const p=products[i],existing=cart.find(x=>x.name===p.name&&x.size===p.size);if(existing)existing.qty++;else cart.push({...p,qty:1});save();openCart()}function save(){localStorage.setItem('danki-cart',JSON.stringify(cart));updateCart()}function updateCart(){document.getElementById('count').textContent=cart.reduce((s,x)=>s+x.qty,0);const el=document.getElementById('cartItems');el.innerHTML=cart.length?cart.map((p,i)=>`<div class="cart-item"><img src="${p.img}" alt=""><div><h4>${p.name}</h4><p>${p.size}</p><b>${money(p.price*p.qty)}</b><div class="qty"><button onclick="change(${i},-1)">−</button> ${p.qty} <button onclick="change(${i},1)">+</button></div></div><button onclick="removeItem(${i})" style="border:0;background:none">×</button></div>`).join(''):'<p style="padding:25px;color:var(--muted)">Your bag is waiting for something beautiful.</p>';document.getElementById('total').textContent=money(cart.reduce((s,x)=>s+x.price*x.qty,0))}function change(i,n){cart[i].qty+=n;if(cart[i].qty<=0)cart.splice(i,1);save()}function removeItem(i){cart.splice(i,1);save()}function openCart(){document.getElementById('drawer').classList.add('open');document.getElementById('backdrop').classList.add('open');updateCart()}function closeCart(){document.getElementById('drawer').classList.remove('open');document.getElementById('backdrop').classList.remove('open')}function checkout(){if(!cart.length)return;let total=cart.reduce((s,x)=>s+x.price*x.qty,0);let lines=cart.map(x=>`☕ ${x.name} — ${x.size} × ${x.qty}`).join('%0A');let msg=`Hello Danki Coffee! I'd like to place an order:%0A%0A${lines}%0A%0ATotal: ${money(total)}%0A%0APlease confirm availability and delivery.`;window.open('https://wa.me/255744600042?text='+msg,'_blank')}renderProducts();updateCart();